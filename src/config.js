// DEMO ONLY — this value is completely fake and not a real secret.
// It is here intentionally so VoiceOps Guard can demonstrate secret scanning.
const config = {
  appName: "voiceops-demo-app",
  port: 3000,
  // ⚠️ FAKE DEMO VALUE — NOT A REAL KEY. Safe to commit.
  apiKey: "sk_demo_FAKE_NOT_REAL_123456789",
};

module.exports = config;
